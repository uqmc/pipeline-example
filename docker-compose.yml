version: "3.7"

services:
    nginx:
      image: nginx
      ports: ["8080:80"]
      restart: unless-stopped
      depends_on: [web]
      volumes:
          - ./nginx.conf:/etc/nginx/conf.d/pipeline-example.conf

    web:
      build: .
      restart: unless-stopped